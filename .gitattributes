# Normalize line endings: store LF in the repo regardless of OS
* text=auto eol=lf

# Windows-only scripts keep CRLF
*.ps1 text eol=crlf
*.bat text eol=crlf

# Binary files: never touch
*.parquet binary
*.xlsx binary
*.xls binary
*.png binary
*.gif binary
